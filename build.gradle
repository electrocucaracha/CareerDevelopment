apply plugin: 'java'
apply plugin: 'eclipse'

version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'Career Development API',
                   'Implementation-Version': version
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.hibernate', name: 'hibernate-core', version: '5.+'
    compile group: 'org.springframework.data', name: 'spring-data-jpa', version: '1.10.+'
    runtime group: 'com.h2database', name: 'h2', version: '1.4.+'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
